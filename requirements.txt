# HPC install: pip install -r requirements.txt
# Pinned for the remdm311 conda env on Bocconi HPC (Python 3.11, CUDA 12.8, A100).
# Takes precedence over pyproject.toml for HPC runs.
pyyaml>=6.0
numpy>=1.24
tqdm>=4.66
rich>=13.7
torch>=2.1
setuptools<70  # pkg_resources needed by lightning 2.2.1 (removed in setuptools 70+)
transformers==4.38.2  # must match checkpoint; newer versions moved tokenization_gpt2_fast
hydra-core==1.3.2
omegaconf>=2.3
datasets>=2.21  # >=2.21 required for NumPy 2.x compat (np.array copy= behaviour)
einops>=0.7
lightning>=2.2
wandb>=0.16  # >=0.16 required for NumPy 2.x compat (np.float_ removed)
h5py>=3.10
timm>=0.9
mauve-text>=0.4

# dev
pytest>=7.4
ruff>=0.3
black>=24.1
isort>=5.13
pre-commit>=3.6
